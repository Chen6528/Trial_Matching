# Base URL of the FastAPI backend (no trailing slash). The browser calls this
# directly, so it must be reachable from the user's machine and listed in the
# backend's CORS_ORIGINS.
#   local:      http://localhost:8000
#   production: https://your-backend.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
